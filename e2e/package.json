{
  "name": "ade-e2e",
  "private": true,
  "type": "module",
  "description": "WebDriver (tauri-driver) E2E harness for ADE — runs against the real binary on Linux.",
  "scripts": {
    "test": "wdio run ./wdio.conf.mjs"
  },
  "devDependencies": {
    "@wdio/cli": "^9",
    "@wdio/local-runner": "^9",
    "@wdio/mocha-framework": "^9",
    "@wdio/spec-reporter": "^9"
  }
}
